object Question01_02and03 {
  def main(args: Array[String]) = {

    // declaring variables
    // integers
    var k, i, j = 2;
    var m, n = 5;

    // floats
    var f = 12.0f;
    var g = 4.0f;

    // chars
    var c = 'X';

    println("a) k + 12 * m = " + (k + 12 * m));
    println("b) m / j = " + (m / j));
    println("c) n % j = " + (n % j));
    println("d) m / j * j = " + (m / j * j));
    println("e) f + 10*5 + g = " + (f + 10 * 5 + g));
    i += 1;
    println("f) ++i * n = " + (i * n));

  }
}
